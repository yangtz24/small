server:
  port: 9001
spring:
  application:
    name: oauth-service
  redis:
    host: 123.56.143.183
    port: 6379
    password: redis123
    jedis:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
    database: 3
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://139.155.56.176:3306/shop_mall?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: Wo123456
  main:
    allow-bean-definition-overriding: true
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka
auth:
  ttl: 3600  #tSUCCESSen存储到redis的过期时间
  clientId: mallshopping
  clientSecret: mallshopping
  cookieDomain: localhost
  cookieMaxAge: -1
encrypt:
  key-store:
    location: classpath:/mallshopping.jks
    secret: mallshopping
    alias: mallshopping
    password: mallshopping